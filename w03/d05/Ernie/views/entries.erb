  <% @entries.each do |entry| %>
    <section>
      <a href="<%="/entries/#{entry.id}"%>"><img src='<%= entry.photo %>'></a>
    <h3>Shot by <%= entry.author %> on <%= entry.post_date %></h3>
    </section>
  <% end %>