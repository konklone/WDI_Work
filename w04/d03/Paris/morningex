def encode plaintext
  plain = alpha = ("a".."z").to_a
  cipher = plain.rotate(13)
  plaintext_array = plaintext.split(//)
  ciphertext_array = plaintext_array.map do |char| 
    index = plain.index(char)
    cipher[index]
  end 

ciphertext_array.join("")

end


encode"jane"
=> "wnar"