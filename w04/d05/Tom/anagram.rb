# length of the word
# if the contents or members of the array are the same, regardless of order
# Compare .map on the array and .include? on the string if one of the combinations works (an or statment) then true is returned and the member of the array whould be returned


#check length first if they don't have the same ampunt of letters they are removed for possibility
# .sort is an array method that sorts alpha

word = 'listen'

choice_array = ['enlists', 'google', 'inlets', 'banana']


def anagram



end

 if
 choice_array.map { |x|  x.chars }.include?