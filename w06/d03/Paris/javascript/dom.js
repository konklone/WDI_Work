window.onload = function(){
  confirm("Are we ready?")
};

var cheesy = document.getElementById("add-cheese"); {
  cheesy.onlick = function(event){alert("now we're ready!");
};

var buildBackground = function)() {
  var e = document.getElementById("lightbox");
  e.style.backgroundImage= "url(''

}
};


// want to wait until the window is loaded before we start messing with the dom.