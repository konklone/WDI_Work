<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <h2><%= link_to student.name, students_path(student) %> </h2>
    <img src="<%= student.img_url %>"/>
  </div>
  <% end %>


