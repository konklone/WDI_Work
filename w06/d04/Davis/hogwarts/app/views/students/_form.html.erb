<%= form_for @student do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :image_url %>
  <%= f.text_field :img_url %>

  <%= f.hidden_field :house_id, value: @house_id %>

  <%= f.submit %>
<% end %>