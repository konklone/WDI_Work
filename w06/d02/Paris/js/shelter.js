function Shelter(name) {
  this.name = name;
  this.animals = [];
}

var happitails = {
  name: "happitails",
  animal: function Animal(name, type) {
   this.name = name;
    this.type = type;
  },
  adopt: function() { 
    animals = []
    animals << animal;
  },
  toString: function() {
    return this.name + " has " + animals.length + "up for adoption.";
  },
//   hasAnimal: function() {
//     this.hasAnimal = true;
//   }
// }

function Animal(name, type) {
  this.name = name;
  this.type = type;
}

var lincoln = new Animal("Lincoln", "dog");


Animal.prototype.speak = function() {
  return "Hello, I am " + this.name;
}

Shelter.prototype.toString = function() {
  return this.name + " has " + animals.length + "up for adoption.";
};

Shelter.prototype.animals = [];

happitails.rescue(lincoln);

// 

homework

prompt()

var response = prompt("hello")




